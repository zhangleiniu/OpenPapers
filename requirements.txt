# Core dependencies
requests>=2.28.0
beautifulsoup4>=4.11.0
python-dotenv>=1.0.0

# Google Cloud / Vertex AI (required for AAAI, ACL, EMNLP, NAACL, IJCAI scrapers)
# Provides the `vertexai` package used in utils.create_gemini_model()
# If you only need scrapers that don't use LLM filtering (NeurIPS, ICML, ICLR,
# CVPR, ICCV, ECCV, COLT, UAI, AISTATS, JMLR), this dependency is optional.
google-cloud-aiplatform>=1.60.0