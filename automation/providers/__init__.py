"""Optional live discovery providers.

Provider modules must be imported only by explicit automation entry points so
the core scrapers and deployed deterministic monitor keep their dependency
boundary.
"""
